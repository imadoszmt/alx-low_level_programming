#include "main.h"

/**
 * _strcmp - a function that compares two strings "s1" and "s2".
 * @s1: a pointer to character.
 * @s2: a pointer to character.
 *
 * Return: integer
 */

int _strcmp(char *s1, char *s2)
{
	int i, j;

	for (i, j; s[i] != '\0' || s[j] != '\0'; i++, j++)
	{
		if (s[i] > s[j])
		{
			return (15);
		}
		else if (s[j] < s[j])
		{
			return (-15);
		}
		else
			return (0);
	}
}
