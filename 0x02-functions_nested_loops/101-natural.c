#include <stdio.h>

/**
 * main - Print all multiples of "3" below 1024 (excluded).
 *
 * Return: Integer.
 */

int main(void)
{
	int n;
	int sum;

	for (n = 3; n < 1024; n += 3)
	{
		sum = n
	}
	printf("%d\n", sum);
}
