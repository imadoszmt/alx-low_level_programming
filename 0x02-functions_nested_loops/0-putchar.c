#include "main.h"

/**
 * main - a program that prints "_putchar".
 *
 *Return: 0 (Success)
 */

int main(void)
{
	char str[] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};
	
	for (int ch = 0, str[ch] <=7 , ch++)
	{
		_putchar(str[ch]);
	}
	
	putchar('\n');
	return (0);
}
